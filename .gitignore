# Environment — contains Google OAuth client credentials
.env

# Python
venv/
__pycache__/
*.pyc
*.pyo
.pytest_cache/
*.egg-info/

# Database — local SQLite email cache
*.db
*.sqlite

# OAuth tokens — stored at ~/.gmail-cleanup/token.json (outside this repo)
# No entry needed here; that path is never inside the project directory.

# Node
node_modules/
frontend/dist/

# OS
.DS_Store
